---
phase: 08-documentation-portfolio
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - workflows/contact-form-ai.json
  - workflows/README.md
autonomous: true

must_haves:
  truths:
    - "Workflow JSON contains sticky notes explaining each major section (input, validation, AI, routing, logging, notifications)"
    - "Exported workflow JSON can be imported into a fresh n8n instance without errors (credentials excluded, structure valid)"
    - "workflows/README.md documents all 4 required credential types with exact setup steps"
  artifacts:
    - path: "workflows/contact-form-ai.json"
      provides: "Annotated workflow with sticky notes and clean export"
      contains: "Sticky Note"
    - path: "workflows/README.md"
      provides: "Detailed import and credential setup instructions"
      min_lines: 50
  key_links:
    - from: "workflows/README.md"
      to: "workflows/contact-form-ai.json"
      via: "import instructions referencing the JSON file"
      pattern: "contact-form-ai\\.json"
---

<objective>
Add self-documenting sticky notes to the n8n workflow canvas and prepare a clean, importable workflow JSON with comprehensive credential setup documentation.

Purpose: Sticky notes make the workflow self-explanatory when viewed in n8n's visual editor (DOCS-03). Clean export ensures anyone can import the workflow (DOCS-01). Detailed credential docs prevent the #1 import failure: missing/misconfigured credentials.

Output: Annotated workflow JSON with 5-6 sticky notes, enhanced workflows/README.md with credential setup table and troubleshooting.
</objective>

<execution_context>
@C:/Users/Eagi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Eagi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior phase context
@.planning/phases/07-error-handling-testing/07-01-SUMMARY.md
@.planning/phases/08-documentation-portfolio/08-RESEARCH.md

# Source files
@workflows/contact-form-ai.json
@workflows/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sticky notes to workflow JSON</name>
  <files>workflows/contact-form-ai.json</files>
  <action>
Add 5-6 n8n Sticky Note nodes to workflows/contact-form-ai.json. Each sticky note is a node with type "n8n-nodes-base.stickyNote" and typeVersion 1. Position them near the relevant node groups.

Sticky notes to add (with Markdown content):

1. **Workflow Overview** (position near top-left, before webhook):
   ```
   ## Contact Form AI Automation
   Receives form submissions via webhook, analyzes with OpenAI, routes spam vs legitimate, logs to Google Sheets, and sends Slack/email notifications.
   **Input:** POST with {name, email, subject, message}
   **Output:** AI analysis with category, summary, draft reply
   ```

2. **Input & Validation** (position near Webhook + Set nodes):
   ```
   ## Input Validation
   Webhook receives POST, Set node extracts fields.
   Required: name, email, message
   Optional: subject
   Auth: Header token (X-Webhook-Auth)
   ```

3. **AI Analysis** (position near OpenAI + Parse AI Response + AI Fallback):
   ```
   ## AI Processing
   OpenAI classifies the inquiry and generates:
   - **Category:** support / sales / feedback / spam
   - **Spam score:** 0-100%
   - **Summary:** One-line description
   - **Draft reply:** Suggested response
   If OpenAI fails, fallback defaults are injected (category: general_inquiry, spam_score: 0).
   ```

4. **Spam Routing** (position near Switch node):
   ```
   ## Spam Detection
   Switch routes by spam_score:
   - **> 70%** → Spam branch (flag, log, skip notifications)
   - **<= 70%** → Legitimate branch (log, notify, respond)
   ```

5. **Logging & Notifications** (position near Google Sheets, Slack, Email):
   ```
   ## Logging & Notifications
   **Google Sheets:** All submissions logged (spam flagged)
   **Slack:** Rich notification with color-coded category
   **Email:** HTML notification to configured recipient
   **Warnings:** Tracks partial failures (AI/Slack/Email down)
   Notification failures never block the form response.
   ```

Each sticky note node structure:
```json
{
  "id": "sticky-note-N",
  "name": "Sticky Note - [Section Name]",
  "type": "n8n-nodes-base.stickyNote",
  "typeVersion": 1,
  "position": [X, Y],
  "parameters": {
    "content": "[markdown content above]",
    "width": 320,
    "height": 220
  }
}
```

Position sticky notes ABOVE or to the LEFT of their related node groups, offset by -200 on Y axis from the nearest related node. Use the existing node positions in the JSON to determine placement. Do NOT add any connections to/from sticky notes (they are visual-only).

Verify the resulting JSON is valid with `node -e "JSON.parse(require('fs').readFileSync('workflows/contact-form-ai.json', 'utf8'))"`.
  </action>
  <verify>
1. `node -e "const w = JSON.parse(require('fs').readFileSync('workflows/contact-form-ai.json', 'utf8')); const stickies = w.nodes.filter(n => n.type.includes('stickyNote')); console.log('Sticky notes:', stickies.length); stickies.forEach(s => console.log('-', s.name))"` — shows 5+ sticky notes
2. JSON is valid (no parse errors)
3. No connections reference sticky note IDs
  </verify>
  <done>Workflow JSON contains 5+ sticky notes with descriptive Markdown content positioned near relevant node groups. JSON is valid and parseable.</done>
</task>

<task type="auto">
  <name>Task 2: Clean workflow JSON for export and enhance credential docs</name>
  <files>workflows/contact-form-ai.json, workflows/README.md</files>
  <action>
**Part A: Verify workflow JSON is clean for export**

Read workflows/contact-form-ai.json and verify:
1. No actual API keys, tokens, or passwords in the JSON (credential values should be references, not plaintext)
2. Credential references use generic names (not revealing internal systems)
3. The workflow name is professional: "Contact Form AI Automation"
4. All node names are descriptive (not default "HTTP Request", "Set", etc.)
5. No test-specific data hardcoded (URLs should use the standard webhook path)

If any issues found, fix them. If clean, document the verification.

**Part B: Rewrite workflows/README.md**

Replace the current basic README with comprehensive import and credential documentation:

```markdown
# n8n Workflow: Contact Form AI Automation

## Quick Import

1. Start n8n: `npm run n8n` (opens at http://localhost:5678)
2. Log in with your n8n credentials
3. Go to **Workflows** > **Import from File**
4. Select `contact-form-ai.json`
5. The workflow imports with 17 nodes + 5 sticky notes

## Required Credentials

Before activating the workflow, create these credentials in **Settings > Credentials**:

| # | Credential Type | Name in Workflow | Setup Steps |
|---|----------------|------------------|-------------|
| 1 | **Header Auth** | Webhook Header Auth | Create new Header Auth credential. Set Header Name: `X-Webhook-Auth`, Header Value: generate a secure token (e.g., `openssl rand -hex 32`). Copy the same token to your `.env` file as `WEBHOOK_AUTH_TOKEN`. |
| 2 | **OpenAI API** | OpenAI API | Create new OpenAI credential. Paste your API key from [platform.openai.com/api-keys](https://platform.openai.com/api-keys). Requires GPT-4 access. |
| 3 | **Google Sheets OAuth2** | Google Sheets account | Create new Google Sheets credential. Follow OAuth2 flow to authorize your Google account. Create a spreadsheet named "Contact Form Submissions" with columns: Timestamp, Name, Email, Subject, Message, Category, Summary, Draft Reply, Spam Score, Is Spam, Warnings. |
| 4 | **Slack API** | Slack Bot Token | Create a Slack app at [api.slack.com/apps](https://api.slack.com/apps). Add `chat:write` scope. Install to workspace. Copy Bot User OAuth Token to credential. Set channel in the Slack node. |

## After Import Checklist

- [ ] Create all 4 credentials above
- [ ] Assign credentials to nodes (click each node, select the credential)
- [ ] Update Google Sheets node with your spreadsheet name/ID
- [ ] Update Slack node with your channel name
- [ ] Update Email node with recipient address
- [ ] Activate the workflow (toggle in top-right)
- [ ] Test with: `curl -X POST http://localhost:5678/webhook/contact-form -H "Content-Type: application/json" -H "X-Webhook-Auth: YOUR_TOKEN" -d '{"name":"Test","email":"test@example.com","subject":"Test","message":"Hello"}'`

## Troubleshooting

**"Credential not found" error on import:**
This is normal. n8n exports credential names but not secrets. Create the credentials first, then assign them to the nodes.

**Workflow doesn't trigger:**
1. Ensure workflow is active (green toggle)
2. Check webhook URL matches what your form sends to
3. Verify X-Webhook-Auth header value matches

**OpenAI returns errors:**
1. Verify API key is valid and has GPT-4 access
2. Check account has sufficient credits
3. AI fallback handler will inject defaults if OpenAI is down

## Workflow Structure

17 processing nodes + 5 annotation sticky notes:

- **Trigger:** Webhook (POST /contact-form)
- **Validation:** Set node extracts and normalizes fields
- **AI Analysis:** OpenAI node + Parse AI Response + AI Fallback Handler
- **Routing:** Switch node (spam score > 70% → spam branch)
- **Logging:** Google Sheets (all submissions, spam flagged)
- **Notifications:** Slack + Email (legitimate only)
- **Error Handling:** continueOnFail on all external nodes, Build Warnings tracks failures
```

Adapt the above to match the actual state of the workflow (node names, credential names as they appear in the JSON).
  </action>
  <verify>
1. `grep -c "sk-" workflows/contact-form-ai.json` — returns 0 (no API keys)
2. `grep -c "xoxb-" workflows/contact-form-ai.json` — returns 0 (no Slack tokens)
3. workflows/README.md exists with credential table and troubleshooting section
4. `wc -l workflows/README.md` — at least 50 lines
  </verify>
  <done>Workflow JSON verified clean of secrets. workflows/README.md contains comprehensive import instructions, credential setup table with 4 credential types, after-import checklist, and troubleshooting section.</done>
</task>

</tasks>

<verification>
1. Workflow JSON is valid and parseable
2. 5+ sticky notes present with descriptive Markdown content
3. No secrets or API keys in workflow JSON
4. workflows/README.md has credential setup for all 4 credential types
5. workflows/README.md has troubleshooting section
</verification>

<success_criteria>
- Workflow JSON imports into n8n with sticky notes visible on canvas
- All credential types documented with step-by-step setup
- No secrets leaked in exported workflow JSON
- workflows/README.md is comprehensive enough for a stranger to set up the workflow
</success_criteria>

<output>
After completion, create `.planning/phases/08-documentation-portfolio/08-01-SUMMARY.md`
</output>
