---
phase: 03-ai-processing-core
plan: 03
type: execute
wave: 2
depends_on: [03-01, 03-02]
files_modified: []
autonomous: false

user_setup:
  - service: openai
    why: "AI classification and response generation"
    env_vars:
      - name: OPENAI_API_KEY
        source: "OpenAI Dashboard -> API keys -> Create new secret key (https://platform.openai.com/api-keys)"
    dashboard_config:
      - task: "Create OpenAI credential in n8n"
        location: "n8n UI -> Credentials -> Add Credential -> OpenAI API -> paste API key"

must_haves:
  truths:
    - "Form submission reaches OpenAI node and returns classified response"
    - "Success card displays real AI-generated category and summary"
    - "Spam test message returns high spam_score with reason"
    - "Legitimate test message returns appropriate category with draft response"
    - "Parse errors gracefully fall back to defaults (no workflow crash)"
  artifacts: []
  key_links:
    - from: "HTML form POST"
      to: "n8n webhook -> OpenAI -> Parse -> Success Response"
      via: "Full request/response cycle"
      pattern: "category.*summary.*spam_score"
---

<objective>
Verify the complete AI processing pipeline works end-to-end: form submission -> n8n webhook -> OpenAI classification -> parsed response -> frontend display.

Purpose: Confirm that all Phase 3 work (Plans 01 + 02) functions correctly together. This is the integration test -- individual node configuration may be correct but the full pipeline could have wiring issues, credential problems, or response format mismatches.

Output: Verified working pipeline with test submission results confirming all 4 requirements (WKFL-03 through WKFL-06).
</objective>

<execution_context>
@C:\Users\Eagi\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Eagi\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ai-processing-core/03-01-SUMMARY.md
@.planning/phases/03-ai-processing-core/03-02-SUMMARY.md
@workflows/contact-form-ai.json
@public/script.js
@public/index.html
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: End-to-end AI processing verification</name>
  <what-built>
  Plans 03-01 and 03-02 built the complete AI processing pipeline:
  - OpenAI node in n8n workflow with multi-task classification prompt
  - Parse AI Response code node with JSON validation and fallbacks
  - Frontend success card displaying AI category and summary

  Before testing, you need to:
  1. Set up OpenAI API key (if not already done)
  2. Import the updated workflow into n8n
  3. Create the OpenAI credential in n8n
  </what-built>
  <how-to-verify>
  **Setup (one-time):**
  1. Start n8n: `npm run n8n` (opens at http://localhost:5678)
  2. Import workflow: n8n UI -> Workflows -> Import from File -> select `workflows/contact-form-ai.json`
  3. Create OpenAI credential: n8n UI -> Credentials -> Add Credential -> OpenAI API -> paste your OPENAI_API_KEY
  4. Open the "Analyze Contact Form" node -> select your OpenAI credential from the dropdown
  5. Create Header Auth credential: n8n UI -> Credentials -> Add Credential -> Header Auth -> Name: "X-Webhook-Auth", Value: "demo-token-2026"
  6. Activate the workflow (toggle in top-right)
  7. Start form server: `npm run dev` (opens at http://localhost:3000)

  **Test 1: Legitimate support message**
  - Fill form: Name="Jane Smith", Email="jane@example.com", Subject="Can't log in to my account", Message="I've been trying to log in for the past hour but keep getting an 'invalid password' error. I've tried resetting my password twice but the reset email never arrives. This is urgent as I need to access my reports before tomorrow's meeting."
  - Submit and observe success card
  - Expected: Category should be "support", Summary should mention login/password issue
  - Check n8n execution history: should show OpenAI node output with full JSON (category, summary, spam_score near 0, draft_response with empathetic tone)

  **Test 2: Sales inquiry**
  - Click "Send another message"
  - Fill form: Name="Mike Johnson", Email="mike@bigcorp.com", Subject="Enterprise pricing", Message="We're a 500-person company looking to switch from our current solution. Can you send over pricing for your enterprise tier? We'd also like to schedule a demo for our IT team next week."
  - Expected: Category should be "sales", Summary should mention pricing/enterprise
  - Check n8n execution: draft_response should have enthusiastic tone

  **Test 3: Spam message**
  - Click "Send another message"
  - Fill form: Name="SEO Expert", Email="boost@spammy.xyz", Subject="Boost your Google rankings NOW!!!", Message="Hi, I noticed your website is not ranking on the first page of Google. Our team of SEO experts can guarantee first-page rankings within 30 days! Visit http://cheap-seo-deals.biz for a FREE audit. Act now - limited time offer!!!"
  - Expected: Category should be "spam", Summary should mention SEO/spam
  - Check n8n execution: spam_score should be > 80, draft_response should be empty string

  **Test 4: Success card display**
  - For each test above, confirm the success card shows:
    - "Category" row with the AI-assigned category (e.g., "support", "sales", "spam")
    - "Summary" row with a meaningful one-line summary
  - Spam submission should show the same card (no tipoff to spammers)

  **Verification checklist:**
  - [ ] OpenAI node executes without errors
  - [ ] Category classification matches expected (support, sales, spam)
  - [ ] Summary is a meaningful one-liner (not "Contact form submission received")
  - [ ] Spam score is near 0 for legitimate messages, > 80 for spam
  - [ ] Draft response exists for legitimate messages, empty for spam
  - [ ] Success card displays category and summary correctly
  - [ ] No console errors in browser developer tools
  </how-to-verify>
  <resume-signal>Type "approved" if all tests pass, or describe specific issues (e.g., "classification was wrong for test 2" or "success card shows undefined")</resume-signal>
</task>

</tasks>

<verification>
All verification is performed by the human during the checkpoint task above.
</verification>

<success_criteria>
- At least 3 test submissions processed correctly by OpenAI
- Category classification matches expected for support, sales, and spam messages
- Spam detection correctly identifies obvious spam (score > 80)
- Success card displays real AI analysis (not mock values)
- No workflow errors or frontend console errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-ai-processing-core/03-03-SUMMARY.md`
</output>
