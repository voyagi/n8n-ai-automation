---
phase: 06-notification-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [workflows/contact-form-ai.json]
autonomous: false
user_setup:
  - service: slack
    why: "Rich Slack notification with Block Kit formatting"
    env_vars:
      - name: SLACK_BOT_TOKEN
        source: "Slack App → OAuth & Permissions → Bot User OAuth Token (xoxb-...)"
    dashboard_config:
      - task: "Create a Slack App at api.slack.com/apps with bot token scopes: chat:write"
        location: "api.slack.com/apps → Create New App → From Scratch"
      - task: "Install app to workspace and copy Bot Token"
        location: "Slack App → Install App → Install to Workspace"
      - task: "Create #notifications channel (or choose existing channel)"
        location: "Slack workspace → Create Channel"
  - service: smtp
    why: "Email notification with HTML AI analysis"
    env_vars:
      - name: SMTP_HOST
        source: "Email provider SMTP settings (e.g., smtp.mailtrap.io for testing)"
      - name: SMTP_PORT
        source: "Usually 587 (TLS) or 465 (SSL)"
      - name: SMTP_USER
        source: "Email provider account credentials"
      - name: SMTP_PASS
        source: "Email provider account credentials"
    dashboard_config:
      - task: "Create Mailtrap account for SMTP testing (or use existing SMTP provider)"
        location: "mailtrap.io → Sign Up → Email Testing → SMTP Settings"

must_haves:
  truths:
    - "Legitimate submission sends Slack notification with color-coded sentiment bar"
    - "Slack notification displays contact info, category, summary, and draft reply"
    - "Legitimate submission sends HTML email notification with AI analysis"
    - "Spam submission triggers zero notifications (no Slack, no email)"
    - "Notification failures do not break the HTTP response to the user"
  artifacts:
    - path: "workflows/contact-form-ai.json"
      provides: "Slack and Email notification nodes with retry config"
      contains: "Send Slack Notification"
    - path: "workflows/contact-form-ai.json"
      provides: "Email notification node"
      contains: "Send Email Notification"
  key_links:
    - from: "Flag as Legitimate"
      to: "Send Slack Notification"
      via: "connection in legitimate branch output array"
      pattern: "Flag as Legitimate.*Send Slack Notification"
    - from: "Flag as Legitimate"
      to: "Send Email Notification"
      via: "connection in legitimate branch output array"
      pattern: "Flag as Legitimate.*Send Email Notification"
    - from: "Flag as Spam"
      to: "Spam Response"
      via: "spam branch bypasses notification nodes entirely"
      pattern: "Flag as Spam.*Spam Response"
---

<objective>
Add Slack and email notification nodes to the legitimate submission branch of the n8n workflow, with color-coded sentiment indicators, rich Block Kit formatting, HTML email templates, and retry configuration.

Purpose: Demonstrate real-time notification capability — a key selling point for Upwork clients who need their teams alerted when leads arrive. Notifications only fire for legitimate submissions; spam is silently logged.

Output: Updated workflow JSON with 2 new nodes (Slack + Email), wired in parallel from "Flag as Legitimate" alongside existing connections.
</objective>

<execution_context>
@C:/Users/Eagi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Eagi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-notification-system/06-RESEARCH.md
@.planning/phases/05-storage-integration/05-01-SUMMARY.md
@workflows/contact-form-ai.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Slack and Email notification nodes to workflow JSON</name>
  <files>workflows/contact-form-ai.json</files>
  <action>
Add two new nodes to the workflow JSON and rewire the legitimate branch connections.

**Node 1: "Send Slack Notification"** (n8n-nodes-base.slack, typeVersion 2.2)
- id: "slack-notification"
- position: [1860, 200] (above the Google Sheets node at [1860, 390])
- messageType: "attachment" (NOT "block" — only attachment supports color bars)
- text: "New contact form submission from {{ $json.name }}" (fallback text, REQUIRED)
- Channel: use channelId with mode "list" and placeholder value "C0123456789" (user will configure via n8n UI)
- attachments array with single attachment object:
  - color: expression mapping sentiment → Slack color codes:
    `{{ $json.sentiment === 'positive' ? 'good' : ($json.sentiment === 'negative' ? 'danger' : 'warning') }}`
    NOTE: The workflow does NOT have a "sentiment" field from Parse AI Response. The AI prompt does not return sentiment. Use category-based coloring instead:
    `{{ $json.category === 'sales' ? 'good' : ($json.category === 'spam' ? 'danger' : 'warning') }}`
    WAIT — spam won't reach this node (only legitimate branch). Use:
    `{{ $json.category === 'sales' ? 'good' : ($json.category === 'support' ? 'danger' : 'warning') }}`
    - sales = good (green) — revenue opportunity
    - support = danger (red) — needs attention
    - feedback/general_inquiry = warning (yellow) — informational
  - blocks array with Block Kit elements:
    1. Header block: "New Contact Form Submission"
    2. Section with 2 fields: Name (from $json.name), Email (from $json.email)
    3. Section with 2 fields: Category (from $json.category), Subject (from $json.subject)
    4. Section: AI Summary (from $json.summary)
    5. Section: Suggested Reply (from $json.draft_response)
    6. Divider
    7. Context block: Submitted timestamp (from $json.submittedAt)
- otherOptions.includeLinkToWorkflow: false
- credentials: slackApi with placeholder id "slack-bot-cred" and name "Slack Bot Token"
- Node-level retry: retryOnFail: true, maxTries: 3, waitBetweenTries: 5000
- continueOnFail: true (notification failure must NOT break HTTP response)

**Node 2: "Send Email Notification"** (n8n-nodes-base.emailSend, typeVersion 2.1)
- id: "email-notification"
- position: [1860, 580] (below the Google Sheets node)
- fromEmail: "noreply@example.com" (placeholder, user configures)
- toEmail: "admin@example.com" (placeholder, user configures)
- subject: expression "New Contact: {{ $json.name }} - {{ $json.category }}"
- emailFormat: "html"
- html: Full HTML email template with:
  - Blue header bar with "New Contact Form Submission" title
  - Contact info section: Name, Email, Subject, Message
  - AI Analysis section: Category, Spam Score, Summary, Suggested Reply
  - Footer with submission timestamp
  - Inline CSS (no external stylesheets — email clients strip them)
  - All field references use $json.fieldName (data comes from Flag as Legitimate which preserved all fields)
- options.appendAttribution: false
- credentials: smtp with placeholder id "smtp-cred" and name "SMTP Account"
- Node-level retry: retryOnFail: true, maxTries: 3, waitBetweenTries: 5000
- continueOnFail: true

**Connection rewiring:**
Current "Flag as Legitimate" connections:
```json
[
  { "node": "Log to Google Sheets", ... },
  { "node": "Success Response", ... }
]
```
Updated to add both notification nodes in parallel:
```json
[
  { "node": "Log to Google Sheets", "type": "main", "index": 0 },
  { "node": "Success Response", "type": "main", "index": 0 },
  { "node": "Send Slack Notification", "type": "main", "index": 0 },
  { "node": "Send Email Notification", "type": "main", "index": 0 }
]
```

IMPORTANT: Do NOT modify "Flag as Spam" connections. Spam branch must NOT connect to any notification node. This ensures NOTF-03 (spam skips notifications).

Do NOT add connections FROM Slack/Email TO Success Response. These are fire-and-forget parallel nodes. The Success Response already receives its connection from Flag as Legitimate directly. Adding Slack→Response would cause the response to wait for notifications.

After editing, validate the JSON file with `node -e "JSON.parse(require('fs').readFileSync('workflows/contact-form-ai.json','utf8')); console.log('Valid JSON')"`.
  </action>
  <verify>
1. Run JSON validation: `node -e "JSON.parse(require('fs').readFileSync('workflows/contact-form-ai.json','utf8')); console.log('Valid JSON')"`
2. Verify node count: `node -e "const w=JSON.parse(require('fs').readFileSync('workflows/contact-form-ai.json','utf8')); console.log('Nodes:', w.nodes.length)"` — should be 15 (was 13)
3. Verify new nodes exist: `node -e "const w=JSON.parse(require('fs').readFileSync('workflows/contact-form-ai.json','utf8')); console.log(w.nodes.map(n=>n.name).join('\n'))"` — must include "Send Slack Notification" and "Send Email Notification"
4. Verify legitimate branch connections: `node -e "const w=JSON.parse(require('fs').readFileSync('workflows/contact-form-ai.json','utf8')); console.log(JSON.stringify(w.connections['Flag as Legitimate'],null,2))"` — must have 4 targets: Log to Google Sheets, Success Response, Send Slack Notification, Send Email Notification
5. Verify spam branch has NO notification connections: `node -e "const w=JSON.parse(require('fs').readFileSync('workflows/contact-form-ai.json','utf8')); const targets = w.connections['Flag as Spam'].main[0].map(c=>c.node); console.log('Spam targets:', targets); console.assert(!targets.includes('Send Slack Notification'), 'FAIL: Slack on spam branch'); console.assert(!targets.includes('Send Email Notification'), 'FAIL: Email on spam branch'); console.log('PASS: No notifications on spam branch')"`
6. Verify continueOnFail on both notification nodes: `node -e "const w=JSON.parse(require('fs').readFileSync('workflows/contact-form-ai.json','utf8')); w.nodes.filter(n=>n.name.includes('Notification')).forEach(n=>console.log(n.name, 'continueOnFail:', n.continueOnFail, 'retryOnFail:', n.retryOnFail))"`
  </verify>
  <done>
- Workflow JSON has 15 nodes (13 existing + 2 new notification nodes)
- "Send Slack Notification" uses attachment messageType with category-based color coding
- "Send Email Notification" uses HTML format with complete AI analysis template
- Both nodes connected from "Flag as Legitimate" in parallel with existing Log/Response connections
- "Flag as Spam" has zero notification connections
- Both notification nodes have continueOnFail: true and retryOnFail: true (3 tries, 5s wait)
- JSON validates successfully
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify notifications with real credentials</name>
  <files>workflows/contact-form-ai.json</files>
  <action>
Import the updated workflow into n8n, configure real Slack and SMTP credentials, and run three verification tests. Fix any issues found during testing (node configuration, connection wiring, expression errors).

**What was built:** Slack and Email notification nodes added to the legitimate submission branch. Both fire in parallel when a non-spam form is submitted. Spam submissions bypass notifications entirely. Both nodes have retry and continue-on-fail configured.

**Pre-requisites (user setup):**
1. Create Slack App at api.slack.com/apps (bot token scopes: chat:write)
2. Install app to your workspace, copy Bot Token (xoxb-...)
3. Create or choose a Slack channel for notifications (note the channel name)
4. In n8n UI: add Slack API credential with the bot token
5. In n8n UI: configure "Send Slack Notification" node → select your channel
6. Set up SMTP credentials (Mailtrap recommended for testing: mailtrap.io → Email Testing → SMTP Settings)
7. In n8n UI: add SMTP credential with host/port/user/pass
8. In n8n UI: configure "Send Email Notification" node → set From and To email addresses

**Test 1: Legitimate submission sends both notifications**
1. Import updated workflow JSON (or re-import if already imported)
2. Activate the workflow
3. Submit a legitimate message via the contact form (e.g., "Hi, I'd like to learn about your pricing plans")
4. Check Slack channel → notification should appear with:
   - Color-coded left border (green for sales, red for support, yellow for feedback/general)
   - Header: "New Contact Form Submission"
   - Name, Email, Category, Subject fields
   - AI Summary and Suggested Reply sections
   - Submission timestamp
5. Check email inbox (or Mailtrap) → notification should arrive with:
   - Subject: "New Contact: [name] - [category]"
   - HTML formatted with contact info and AI analysis sections

**Test 2: Spam submission sends zero notifications**
1. Submit an obvious spam message (e.g., "BUY CHEAP VIAGRA NOW!!! Click http://spam.com for amazing deals!!!")
2. Verify: NO new Slack notification appears
3. Verify: NO new email notification arrives
4. Verify: Google Sheets still logs the spam submission (with is_spam = TRUE)

**Test 3: Notification failure does not break response**
1. Temporarily break the Slack credential (change token to invalid value)
2. Submit a legitimate message
3. Verify: Form still receives success response (HTTP 200)
4. Verify: n8n execution shows Slack node errored but workflow continued
5. Restore the correct Slack credential
  </action>
  <verify>
1. Slack notification appeared in channel with color-coded border and all AI analysis fields
2. Email notification arrived with HTML formatting and correct subject line
3. Spam submission produced zero notifications (check Slack channel and email inbox)
4. Broken Slack credential did not prevent form from receiving HTTP 200 response
5. Google Sheets still logs entries for both spam and legitimate submissions (no regression)
  </verify>
  <done>
- All 3 tests pass: legitimate notifications sent, spam notifications skipped, failures handled gracefully
- Slack notification has category-based color bar and Block Kit formatting
- Email notification has HTML formatted AI analysis
- No regressions in existing workflow behavior
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. Workflow JSON has 15 nodes total
2. Legitimate submissions produce both Slack and email notifications
3. Slack notification has category-based color coding (green/red/yellow)
4. Email notification has HTML formatting with full AI analysis
5. Spam submissions produce zero notifications
6. Notification failures (bad credentials, network) do not prevent HTTP response
7. Google Sheets logging still works for both branches (no regression)
</verification>

<success_criteria>
- All 5 phase success criteria from ROADMAP are met
- Workflow JSON validates and imports cleanly into n8n
- Parallel notification execution does not slow down HTTP response
- No regressions in spam routing, Google Sheets logging, or form submission flow
</success_criteria>

<output>
After completion, create `.planning/phases/06-notification-system/06-01-SUMMARY.md`
</output>
